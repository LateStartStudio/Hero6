# Install steps

steps:
# git lfs pull
- bash: cd assets && git lfs pull
  displayName: git lfs pull

# Install dotnet SDK
- task: UseDotNet@2
  displayName: Install dotnet SDK
  inputs:
    packageType: 'sdk'
    version: '2.2.401'
