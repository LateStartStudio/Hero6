# Install steps

steps:
# Pull git lfs files
- bash: git lfs install
  displayName: git LFS install
- bash: git lfs pull
  displayName: git LFS pull

# Install dotnet SDK
- task: UseDotNet@2
  displayName: Install dotnet SDK
  inputs:
    packageType: 'sdk'
    version: '2.2.401'
